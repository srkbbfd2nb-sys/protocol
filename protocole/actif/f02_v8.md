# F-02 V.8 — Couche ACTIVE (runtime)

> **Couche** : active (MD). **Classification** : *méta-infrastructure* (orchestration globale — index Noyau corrigé cycle 22). **Statut : DORMANT sauf escalade** (S3-B).
> **Rôle** : orchestrateur de pipeline quand la complexité l'exige. Complémentaire d'ACA : ACA = activation contextuelle par cycle ; F-02 = orchestration globale multi-modules.
> **Détail** (pipeline 8 étapes, OFI, MAO, registre) → `archive/f02_v8_detail.md`.

## AMORCE (explicite — traite le point M0 « amorce d'escalade implicite »)
S'active **uniquement sur escalade** : MEDA Expert / modules ≥ 5 (`08_f02:198`) · fail_safe Noyau (auto) · **F-01 MRS** (SDA-I ≥ 9 — suspension) · rigueur critique (SDA-I 6-8) · dérive DVS (✗) · **MECA rejet** (< 0,50 ou insuffisant) · config MAO (system prompt/API). Sinon : **dormant** — aucun coût runtime.

## PROCESSUS
Ordonnance le pipeline (ordre d'invocation, paramètres de profondeur, mode EEO si actif) → supervise → collecte. **OFI** : sur signal MECA (rejet), relance le cycle de production (itérations bornées) — ré-entrée canonique.

## SORTIE
Ligne ORCHESTRATION ACTIVE · registre des invocations (exportable, log INV.8) · rapport OFI.

## Couplages *(réf. Graphe ; passe fraîche `08_f02:198-199`)*
- **F-02 → tous modules actifs** : `Fn active` (ordre + paramètres). **→ MECA** : `Fn active` (évaluation fin de pipeline + OFI).
- **F-02 ← MECA** : `Fn rétroagit` (rejet → OFI — boucle). **← F-01** : `Fn active` (MRS/critique). **← MEDA** (Expert/≥5, EEO) · **← Noyau** (escalade, fail_safe).

→ Pipeline 8 étapes, itérations OFI, régimes MAO, champs du registre : `archive/f02_v8_detail.md`.
