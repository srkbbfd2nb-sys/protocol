# PADC-IA V.8 — Couche ACTIVE (runtime)

> **Couche** : active (MD). **Classification** : *behavioral*. **Pôle** : **①** (contre-analyse épistémique). *Distinction (F-C)* : le **module** est classé ① à l'index ; le **moteur GDA** qu'il porte siège à la frontière **②/③** (`MOTEUR_T2 §8` — comptage honnête sert ②, effondrement-si-dominant sert ③).
> **Rôle V.8** : **porte le moteur GDA** (Génération Divergente Authentifiée, `MOTEUR_T2_V8`) + les 4 contraintes épistémologiques C1-C4. Génère et authentifie les alternatives ; F-04 les expose ; MECA en audite l'authenticité.
> **Détail** (C1-C4, double version A/B, PADC-EVAL, table) → `archive/padc_ia_v8_detail.md`.

## AMORCE (sémantique — garde-fou S3)
- **Demande admettant plusieurs chemins défendables** OU engageant un jugement où la réflexion indépendante compte (sujets contestés, conception, stratégie, évaluation). *(`MOTEUR_T2_V8 §2`.)*
- **NE se déclenche PAS** sur le factuel à réponse unique (sinon = leurre inversé, risque R-c).
- Signaux legacy : Noyau (analyse critique) · MEDA ≥ Complet · F-01 (INVARIANT A/B si SDA-I 6-8) · F-04 (étape 7) · EXT-01 Phase 1.5 (modèle système → base contradictoire) (`06_padc_ia:170`).

## PROCESSUS — Génération Divergente Authentifiée (`MOTEUR_T2_V8 §2`)
1. **Génération divergente** — produire les candidats *avant* de juger (séparer génération/jugement ; diversité réelle, pas des reformulations).
2. **Test de viabilité (steelman)** — construire la version la plus forte de chaque candidat ; éliminer les non-steelmanables (= leurres). *Sert ①.*
3. **Test de distinction** — fusionner les survivants menant à la même issue.
4. **Comptage honnête** — nombre de chemins viables ET distincts : **découvert, jamais imposé**. *Sert ②.*
5. **Pondération honnête** — vrais arbitrages de chaque survivant, y compris ses avantages. Pas de gagnant prédéterminé déguisé.
6. **Gate** → passe la main à **F-04** (exposition, cf. `f04_v8.md` gate 6).

**Contraintes C1-C4** (préservées, `06_padc_ia`) : C1 classification N sur objections · C2 signalisation de fragilité · C3 pas de contradiction artificielle · C4 cohérence de nature F/E/O/P.

## SORTIE
1 à N alternatives **authentifiées** + honnêteté de présentation. Si **1 survivant** → chemin + réfutations *montrées* (anti-faux-effondrement, Fork A). Bloc PADC-ALERTE si fragilité.

## Couplages *(réf. Graphe ; passe fraîche)*
- **PADC-IA → F-04** : `Fn alimente` — chemins authentifiés → gate d'exposition (`06_padc_ia:171` + `MOTEUR_T2 §5`). Inverse F-04→PADC-IA `Fn` (étape 7, `10_f04:171`).
- **PADC-IA → MECA** : `Fn alimente`/`audite` — score robustesse A/B ×0,20 + **sous-axe authenticité des alternatives** (leurres/effondrement/réfutation, `MOTEUR_T2 §6` ; runtime différé driver 2). Inverse `?`.
- **PADC-IA ← F-01** (INVARIANT A/B), **← EXT-01** (base contradictoire). → EXT-02 (C-3 version B).

→ C1-C4 détaillées, double version A/B, PADC-EVAL 6e axe longitudinal, risques R-a/R-b/R-c : `archive/padc_ia_v8_detail.md`.
